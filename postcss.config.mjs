// [GUIDE: POSTCSS] The project uses plain CSS and needs no PostCSS plugins.
// Keep this empty unless you deliberately add and document a plugin dependency.
const config = { plugins: {} };

export default config;
